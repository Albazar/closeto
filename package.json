{
  "name": "closeto",
  "version": "0.0.7",
  "description": "Number comparison with a tolerance.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepare": "npm run test && npm run build",
    "build": "tsc",
    "test": "jest",
    "coverage": "jest --coverage"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Albazar/closeto.git"
  },
  "author": "albazar",
  "license": "ISC",
  "keywords": [
    "math",
    "precision"
  ],
  "devDependencies": {
    "jest": "^26.5.3",
    "ts-jest": "^26.4.1",
    "typescript": "^4.0.3"
  },
  "jest": {
    "preset": "ts-jest"
  }
}
